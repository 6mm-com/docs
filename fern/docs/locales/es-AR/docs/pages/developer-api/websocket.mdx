---
title: "WebSocket"
description: "Conéctate al servicio 6MM WebSocket para streams en mercados públicos y eventos de cuentas privadas, con guía de heartbeat y suscripción."
slug: es-AR/developer-api/websocket
canonical-url: https://docs.6mm.com/es-AR/developer-api/websocket
---

<h2 id="service-endpoint">Punto final de servicio</h2>

```
wss://ws.6mm.com/ws
```

WebSocket canales se dividen en canales públicos y privados:

| Tipo             | Autenticación              | Descripción                                            |
| ---------------- | --------------------------- | ------------------------------------------------------ |
| Canales públicos  | No es obligatorio                | Datos de mercado, como la profundidad, las operaciones, las Klines y el ticker |
| Canales privados | `listenKey` o JWT requerido | Órdenes, cuentas y flujos de posición de usuario              |

<h2 id="public-channels">Canales públicos</h2>

La WebSocket pública no requiere autenticación. Después de conectarte, envía una solicitud `subscribe` para suscribirte a los canales de mercado.

Canales comunes:

| Canal                            | Descripción                                                        |
| ---------------------------------- | ------------------------------------------------------------------ |
| `market.depth.{symbol}`            | Profundidad del libro de pedidos. Envía primero una instantánea y luego actualizaciones incrementales |
| `market.trade.{symbol}`            | Últimas operaciones                                                      |
| `market.kline.{symbol}.{interval}` | Klines                                                             |
| `market.ticker.{symbol}`           | Resumen del ticker de un solo símbolo                                       |
| `market.tickers`                   | Resumen del ticker de todos los símbolos                                          |

<h2 id="private-channels">Canales privados</h2>

<h2 id="listenkey">listenKey</h2>
Para WebSocket privados de streams, crea primero una `listenKey` a través de la REST API y luego conéctate a:

```
wss://ws.6mm.com/ws?listenKey=YOUR_LISTEN_KEY
```

<h2 id="jwt">JWT</h2>
Método de conexión JWT compatible:

```
wss://ws.6mm.com/ws?token=YOUR_ACCESS_TOKEN
```

Tras el éxito de la autenticación, el servidor suscribe automáticamente la conexión al canal privado del usuario actual. No se requiere suscripción manual.

<h2 id="choose-the-next-guide">Elige la siguiente guía</h2>

- [Conexión, Suscripción y Latido](/es-AR/developer-api/websocket/connection-subscription-and-heartbeat): ciclo de vida de conexión, ping, reconexión y resuscripción.
- [Canales de Mercado Público](/es-AR/developer-api/websocket/public-market-channels): profundidad, operaciones, Klines, tickers y manejo de secuencias.
- [Canal de Usuario Privado](/es-AR/developer-api/websocket/private-user-channel): listenKey ciclo de vida y eventos de cuenta autenticada.
- [Recomendaciones de Integración](/es-AR/developer-api/integration-recommendations): monitorización, idempotencia, pruebas de intentos y pruebas de producción.
