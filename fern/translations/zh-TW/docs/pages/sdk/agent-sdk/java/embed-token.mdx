---
title: "建立嵌入令牌"
subtitle: "發行短命的嵌入代幣 Trading Widget 合作夥伴代幣模式。"
slug: sdk/agent-sdk/java/embed-token
canonical-url: https://docs.6mm.com/zh-TW/sdk/agent-sdk/java/embed-token
---

<h2 id="sdk-call">SDK</h2>

驗證合作夥伴使用者會話後，從後端建立嵌入權杖。瀏覽器應該呼叫你的端點，端點也應該透過Agent SDK呼叫6MM。

```java
CreateEmbedTokenResponse resp = client.createEmbedToken(
        CreateEmbedTokenRequest.of("agent-user-001", "tw_abc123")
                .withSymbol("ETHUSDT"));

System.out.println(resp.embedToken);
System.out.println(resp.expireAt);
```

> **一次性憑證**
>
> embedToken 是短暫且一次性使用的。請勿將其置於 URL 查詢字串中。

<h2 id="backend-endpoint-example">後端端點範例</h2>

```java
@PostMapping("/api/trading/embed-token")
public EmbedTokenView createEmbedToken(@RequestBody EmbedTokenRequest request,
                                       PartnerSession session) {
    String agentUserId = session.getAgentUserId();
    CreateEmbedTokenResponse resp = agentClient.createEmbedToken(
            CreateEmbedTokenRequest.of(agentUserId, request.getChannelId())
                    .withSymbol(request.getSymbol()));

    return new EmbedTokenView(resp.embedToken, resp.expireAt, request.getChannelId());
}
```

僅回傳 Trading Widget 令牌提供者所需的欄位。不要回傳 apiSecret、原始簽名欄位或內部服務設定。

<h2 id="failure-handling">故障處理</h2>

| 狀況 | 建議操作方法 |
| --- | --- |
| 使用者會話遺失或過期 | 從合作夥伴端點回傳 401 並要求使用者重新登入。 |
| 代理使用者不受綁定 | 先綁定使用者，再重新嘗試創建代幣。 |
| 6MM 會回傳一個暫時錯誤 | 把可重試的錯誤回傳到前端，並記錄 requestId 尋求支援。 |
| channelId 與 widget 實例不符 | 拒絕請求並重新建立 widget session。 |

<h2 id="related-documentation">相關文件</h2>

- [Trading Widget 認證 （/sdk/trading-widget/authentication）：將 `tokenProvider` 連接到合作夥伴端點。
- [Java 快速啟動 ]（/sdk/agent-sdk/java/quick-start）： 安裝並初始化 `AgentClient`。
- 【秘密與簽名】（/sdk/security/secrets-signing）：保護特工 API 秘密。
- [Webhooks 與冪性]（/sdk/security/webhooks-idempotency）：保護整合的回調端。
<!-- sixmm-localized-link-repair:start -->
<h2 id="localized-page-links">相關頁面</h2>

- [Trading Widget 認證](/zh-TW/sdk/trading-widget/authentication)
- [Java 快速入門](/zh-TW/sdk/agent-sdk/java/quick-start)
- [Agent SDK 祕密與 HMAC 手語](/zh-TW/sdk/security/secrets-signing)
- [Webhook 簽名驗證與冪性](/zh-TW/sdk/security/webhooks-idempotency)
<!-- sixmm-localized-link-repair:end -->
