---
title: "PHP Início Rápido"
subtitle: "Instale e inicialize o PHP SDK a partir de um serviço backend."
slug: sdk/agent-sdk/php/quick-start
canonical-url: https://docs.6mm.com/pt-PT/sdk/agent-sdk/php/quick-start
---

<h2 id="install">Instalação</h2>

```json
{
  "require": {
    "sixmm/agent-sdk": "VERSION_PROVIDED_BY_6MM"
  }
}
```

> **Acesso ao pacote**
>
> 6MM fornece o nome do pacote Composer aprovado, repositório e versão durante a integração do parceiro. Não se deve assumir que o pacote existe no Packagist público.

<h2 id="before-you-start">Antes de começares</h2>

- Confirma o Agente API base URL com 6MM.
- Carregar apiSecret a partir de variáveis de ambiente ou gestão de segredos.
- Manter SDK utilização apenas dentro dos serviços backend.
- Preparar o manuseamento de idempotentes para transferências e fluxos de webhook.

<h2 id="initialize-client">Inicializar cliente</h2>

```php
<?php

use Sixmm\Agent\AgentClient;
use Sixmm\Agent\AgentConfig;

$client = new AgentClient(new AgentConfig(
    'https://agent-api.6mm.com',
    'AGENT001',
    getenv('AGENT_API_SECRET') ?: '',
    'USDT'
));
```

> **Apenas backend**
>
> O PHP SDK assina pedidos de Agente API e não deve ser exposto através de código-fonte público ou ambientes de execução frontend.

<h2 id="example-transfer-funds-in">Exemplo: transferir fundos em</h2>

```php
$response = $client->transfer([
    'agentOrderNo' => 'AGT-ORDER-1001',
    'agentUserId' => 'agent-user-001',
    'direction' => 'IN',
    'currency' => 'USDT',
    'amount' => '10.00',
]);
```

Use cadeias decimais para os valores e preserve o agentOrderNo original ao tentar novamente ou consultar o estado da transferência.

<h2 id="next-steps">Próximos passos</h2>

| Necessidade | Página seguinte |
| --- | --- |
| Compreenda Agent SDK capacidades | [Agent SDK Visão Geral](/pt-PT/sdk/agent-sdk/overview) |
| Rever o comportamento de assinatura | [Segredos e Sessões de Autógrafos](/pt-PT/sdk/security/secrets-signing) |
| Verificar entrega de retorno de chamada | [Webhooks & Idempotência](/pt-PT/sdk/security/webhooks-idempotency) |
| Diagnosticar um problema de integração | [SDK A Resolver Problemas](/pt-PT/sdk/security/troubleshooting) |
