---
title: "WebSocket"
headline: "6MM WebSocket 实时数据接入"
description: "连接 6MM WebSocket 服务，订阅公开行情和私有账户事件，并正确处理心跳和订阅消息。"
slug: developer-api/websocket
canonical-url: https://docs.6mm.com/zh/developer-api/websocket
---

<h2 id="service-endpoint">服务地址</h2>

```
wss://ws.6mm.com/ws
```

WebSocket 分为公共频道和私有频道：

| 类型   | 鉴权                   | 说明                     |
| ---- | -------------------- | ---------------------- |
| 公共频道 | 不需要                  | 行情数据，如深度、成交、K 线、ticker |
| 私有频道 | 需要 `listenKey` 或 JWT | 用户订单、账户、持仓相关推送         |

<h2 id="public-channels">公共频道</h2>

公共 WebSocket 不需要鉴权，连接后可发送 `subscribe` 请求订阅行情频道。

常用频道：

| 频道                                 | 说明                 |
| ---------------------------------- | ------------------ |
| `market.depth.{symbol}`            | 订单簿深度，订阅后先推快照，再推增量 |
| `market.trade.{symbol}`            | 最新成交               |
| `market.kline.{symbol}.{interval}` | K 线                |
| `market.ticker.{symbol}`           | 单交易对行情摘要           |
| `market.tickers`                   | 全交易对行情摘要           |

<h2 id="private-channels">私有频道</h2>

<h2 id="listenkey">listenKey</h2>

私有 WebSocket 需要先通过 REST 接口创建 `listenKey`，再连接：

```
wss://ws.6mm.com/ws?listenKey=YOUR_LISTEN_KEY
```

<h2 id="jwt">JWT</h2>

兼容 JWT 连接方式：

```
wss://ws.6mm.com/ws?token=YOUR_ACCESS_TOKEN
```

鉴权成功后，服务端会自动订阅当前用户私有频道，无需手动订阅。

<h2 id="choose-the-next-guide">选择下一篇指南</h2>

- [连接订阅与心跳](/zh/developer-api/websocket/connection-subscription-and-heartbeat)：连接生命周期、ping、重连和恢复订阅。
- [公共行情频道](/zh/developer-api/websocket/public-market-channels)：深度、成交、K 线、Ticker 和序列处理。
- [私有用户频道](/zh/developer-api/websocket/private-user-channel)：listenKey 生命周期和账户鉴权事件。
- [接入建议](/zh/developer-api/integration-recommendations)：监控、幂等、重试和生产测试。
