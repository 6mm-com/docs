---
title: "6MM SDKs"
subtitle: "Выберите правильный 6MM SDK путь для встраиваемой торговли, выпуска бэкенд-токенов, привязки аккаунтов, вебхуков и производственных операций."
slug: sdk/overview
canonical-url: https://docs.6mm.com/ru/sdk/overview
---

<h2 id="choose-an-sdk-path">Выберите SDK путь</h2>

<CardGroup cols={3}>
<Card title="Trading Widget SDK" icon="duotone chart-line" href="/ru/sdk/trading-widget/quick-start">
  Внедрите 6MM торговый терминал в существующий сайт, кошелёк, портал брокера или поверхность партнёрского продукта.
</Card>
<Card title="Agent SDK" icon="duotone server" href="/ru/sdk/agent-sdk/overview">
  Подключите партнёрский бэкенд к 6MM для привязки пользователей, встраивания токенов, URL записей, переводов, запросов заказов и вебхуков.
</Card>
<Card title="Безопасность и эксплуатация" icon="duotone shield-check" href="/ru/sdk/security/secrets-signing">
  Подготовьте секретное хранилище, подписание запросов, идентификацию вебхука, мониторинг и устранение неполадок.
</Card>
</CardGroup>

<h2 id="recommended-combination">Рекомендуемая комбинация</h2>

| Партнёрская цель | Фронтенд | Бэкенд | Почему это важно |
| --- | --- | --- | --- |
| Добавьте встроенный торговый терминал | Trading Widget SDK | Agent SDK | Виджет управляет торговым интерфейсом, а партнёрский бэкенд выпускает токены и хранит секреты в тайне. |
| Создайте нативную точку входа в приложение | Нативная или H5-запись | Agent SDK | Приложение открывает 6MM запись URL после того, как партнёр подтвердит пользовательскую сессию. |
| Постройте индивидуальный торговый поток | Партнёрский интерфейс | Разработчик API + выбрал SDK потоки | Используйте APIs для данных о счетах, заказах, позициях и рынке, при этом операционные контроли остаются на серверной стороне. |

<h2 id="reference-architecture">Эталонная архитектура</h2>

```text
Partner Web App
  -> TradingWidget.create()
  -> tokenProvider()
  -> Partner Backend
  -> 6MM Agent SDK
  -> 6MM Agent API
  -> 6MM Trading iframe
```

> **Хранить секреты на стороне сервера**
>
> Код браузера должен вызывать только конечные устройства, принадлежащие партнёрам. Агент API секрет никогда не должен отправляться в фронтенд-код.

<h2 id="production-checklist">Чек-лист производства</h2>

- Хранить apiSecret только в бэкенд-инфраструктуре.
- Используйте HTTPS для запросов API агентов и URL вебхуков.
- Сохранять agentUserId, platformUserIdи agentOrderNo отображения.
- Представлять денежные суммы в виде десятичных строк вместо значений с плавающей точкой.
- Рассматривать PROCESSING как ожидающее, а не как успех или неудачу.
- Обрабатывать вебхуки идемпотентно, потому что ожидаются повторные попытки.
