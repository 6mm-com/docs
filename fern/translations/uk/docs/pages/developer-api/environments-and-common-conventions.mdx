---
title: "Середовища та спільні конвенції"
description: "Переглядайте 6MM API середовища та спільні умови щодо базових URL, часових позначок, числових полів, символів, ідентифікаторів запитів, пагінації та відповідей."
slug: developer-api/environments-and-common-conventions
canonical-url: https://docs.6mm.com/uk/developer-api/environments-and-common-conventions
---

<h2 id="service-endpoints">Кінцеві точки обслуговування</h2>

| Тип      | Кінцева точка              |
| --------- | --------------------- |
| REST API  | `https://api.6mm.com` |
| WebSocket | `wss://ws.6mm.com/ws`    |

<h2 id="time-and-numeric-values">Час і числові значення</h2>

* Якщо не вказано інше, часові поля є мілісекундними часовими мітками Unix.
* Щоденна статистика, балансові знімки та статистика обсягів торгівлі за замовчуванням використовують межі UTC дня.
* Поля, чутливі до точності, такі як кількість, ціна, кількість і швидкість, передаються у вигляді рядків.
* Великі цілочисельні ідентифікатори, такі як `orderId`, `conditionId`, і `strategyId` , повертаються як рядки у JSON.

<h2 id="response-format">Формат відповіді</h2>

Успішна відповідь:

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "requestId": "req-12345678"
}
```

Відповідь на помилку:

```json
{
  "code": 1003,
  "message": "Invalid token",
  "requestId": "req-12345678"
}
```

Примітки:

* Бізнес-помилки зазвичай все одно повертають уніфіковану структуру JSON . Інтегратори повинні спочатку перевірити `code` .
* Невдачі автентифікації можуть повертати HTTP `401`; обмеження швидкості може повертати HTTP `429`.
* Ви можете надіслати `X-Request-ID` у заголовку запиту. Сервер поверне його як `requestId` у відповіді.

<h2 id="related-documentation">Пов'язана документація</h2>

- [Автентифікація](/uk/developer-api/authentication): обирайте JWT або API Key підпис.
- [Підписування запитів](/uk/developer-api/request-signing): створювати та перевіряти підписані запити.
- [REST API](/uk/developer-api/rest-api): застосовують ці умови до кінцевих груп.
- [Коди помилок та усунення несправностей](/uk/developer-api/error-codes-and-troubleshooting): діагностика помилок HTTP та бізнесу.
